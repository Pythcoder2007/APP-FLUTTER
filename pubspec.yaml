name: sports_emporium_manager
description: Mobile billing, inventory, customers, history and reports for Shri Shakra Sports.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.3
  path: ^1.9.1
  intl: ^0.20.3

flutter:
  uses-material-design: true
